import { tv } from "../../app/utils/tv";

export const PurchaseButtonStyles = tv({
  base: [
    "bg-black text-white",
    "flex items-center justify-center",
    "size-[72px]",
    "rounded-full",
    "transition-[height]",
  ],
  variants: {},
});
